symbols = {
    # Numeric constants
    "1️⃣": lambda: 1,
    "2️⃣": lambda: 2,

    # Math
    "➕": lambda a, b: a + b,
    "➖": lambda a, b: a - b,
    "✖️": lambda a, b: a * b,
    "➗": lambda a, b: a / b
}
